# P0.26–P0.34 设计

> 2026-06-12，基于 TASKS.md 未完成项

## 范围

docs/TASKS.md 中 9 个未完成 P0：

| ID | 域 | 标题 |
|----|----|------|
| P0.26 | UI/导出 | popup 完成态按钮拆为查看+导出 |
| P0.27 | UI/导出 | 导出位置分别记忆 |
| P0.28 | UI/导出 | 运行日志默认扩展名 .log |
| P0.29 | 采集数据 | flush_all IndexedDB key path 未 yield 值 |
| P0.30 | 采集数据 | console_events = 0 |
| P0.31 | 采集数据 | network type 全为 unknown |
| P0.32 | 采集数据 | capture 元数据缺 url/tab_title |
| P0.33 | 时间字段 | 导出 JSON 时间字段命名 |
| P0.34 | 时间字段 | 时区选项改为固定 UTC 偏移 |

3 组，每组 1 个 commit。顺序：先文档（TASKS.md），再测试，再代码。

---

## 组 1 — UI/导出

### P0.26 popup 按钮

**文档**：TASKS.md 标记 -> 已修复  
**测试**：`tests/popup_layout.test.ts` — 完成态存在两个按钮 id（查看 + 导出）  
**代码**：
- `src/popup/popup.ts` `render_saved()` 加 `<button id="exportBtn">导出</button>`
- `wire_view()` 绑定 `exportBtn.click` → `chrome.runtime.sendMessage` 触发导出

### P0.27 导出位置记忆

**文档**：TASKS.md 标记 -> 已修复  
**测试**：`tests/export_settings.test.ts` — 两类导出分别读写，互不覆盖  
**代码**：
- `src/shared/types.ts` `UserConfig` 拆 `export_capture_directory` / `export_log_directory`
- `src/shared/constants.ts` 默认值更新
- `src/dashboard/dashboard.ts` 导出完成后 `chrome.storage.local.set` 存目录

### P0.28 .log 扩展名

**文档**：TASKS.md 标记 -> 已修复  
**测试**：`tests/e2e-logging.spec.ts` — 断言 download filename 以 `.log` 结尾  
**代码**：
- `src/dashboard/dashboard.ts` 运行日志 Blob type `text/plain` → `text/x-log`（Chrome 不会覆盖 filename 扩展名）

---

## 组 2 — 采集数据缺陷

### P0.29 flush key path

**文档**：TASKS.md 标记 -> 已修复  
**测试**：新建 `tests/app_log_storage.test.ts` — flush 含空 id 记录不整批失败  
**代码**：
- `src/background/app_log_storage.ts` `write()` + `flush()` 加 `entry.id` 守卫
- `src/background/service_worker.ts` `app_log_batch` handler 过滤缺 id 条目

### P0.30 console_events = 0

**文档**：TASKS.md 标记 -> 已修复  
**测试**：`tests/e2e-console-errors.spec.ts` — 断言 `console_events` 数组非空  
**代码**：
- `src/background/service_worker.ts` `handle_console_log` — `capture_id` 为空时跳过写入

### P0.31 network type unknown

**文档**：TASKS.md 标记 -> 已修复  
**测试**：`tests/network_capture.test.ts` — type 映射正确  
**代码**：
- `src/background/network_capture.ts` `resolve_resource_type` 加 Chrome 内部名→标准名映射

### P0.32 元数据缺 url/tab_title

**文档**：TASKS.md 标记 -> 已修复  
**测试**：新建 `tests/session_manager.test.ts` — 创建后 url/tab_title 非空  
**代码**：
- `src/shared/types.ts` `CaptureRecord` 加 `url?` / `tab_title?`
- `src/background/service_worker.ts` `start_capture` 从 active_tab 取 url + title

---

## 组 3 — 时间字段

### P0.33 导出时间字段命名

**文档**：TASKS.md 标记 -> 已修复  
**测试**：`tests/system_time.test.ts` — 导出 JSON 顶层人读字段存在且不含 `Z`  
**代码**：
- `src/background/exporter.ts` 追加 `*_time_label` 字段

### P0.34 固定 UTC 偏移

**文档**：TASKS.md 标记 -> 已修复  
**测试**：`tests/system_time.test.ts` — `UTC+1`/`UTC+8`/`UTC-5` 格式正确；旧 IANA 迁移  
**代码**：
- `src/shared/types.ts` `SystemTimeTimezone` 扩展为 `'browser' | 'UTC' | 'UTC+1' | ... | 'UTC+12' | 'UTC-1' | ... | 'UTC-12'`
- `src/shared/system_time.ts` 加 `parse_utc_offset()` 辅助
- `src/dashboard/dashboard.ts` 下拉替换为固定 UTC 偏移
- `src/shared/user_config.ts` 旧 `Asia/Shanghai` → `UTC+8`
