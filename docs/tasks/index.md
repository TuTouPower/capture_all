# 任务总清单

- ID 在此分配，全局递增；取 `docs/tasks/` 与 `docs/archive/tasks/` 中最大 ID 加一，无历史时从 T001 开始。
- 状态只使用：`backlog`、`active`、`done`、`dropped`。
- `backlog` 不建目录；`active` 必须有 `TNNN_slug/` 目录。
- `done` 及曾 active 的 `dropped` 任务目录必须移入 `docs/archive/tasks/`。
- owner 和 branch 表示当前归属；工作分支推荐 `task_tnnn_slug`。
- 备注列格式：`commit <sha>` + finding 引用 + 简要问题/修复方向 + 涉及文件。详细分析见 `docs/review_20260719_0859/evaluation.md` 与对应批次 current.md。

| ID | 标题 | 状态 | owner | branch | 备注 |
|----|------|------|-------|--------|------|
| T001 | refactor: align agent docs layout with repo_template (Phase 0) | done | — | task_t001_align_repo_layout | commit `3a34685`；Phase 0 文档骨架 + 入口对齐 |
| T002 | refactor: move agent protocol into src/shared (Phase 2) | done | — | task_t002_shared_protocol_relocate | commit `5c21a50`；扁平 src/shared；extension/shared 下沉留 Phase 3 |
| T003 | refactor: move bridge sources to src/bridge (Phase 3a) | done | — | task_t002_shared_protocol_relocate | commit `e2a7f86` |
| T004 | refactor: move mcp sources to src/mcp (Phase 3b) | done | — | task_t002_shared_protocol_relocate | commit `0be0262`；src/agent/ 已删 |
| T005 | refactor: move extension surfaces + manifest + _locales (Phase 3c) | done | — | task_t002_shared_protocol_relocate | commit `cc399a4` |
| T006 | refactor: sink remaining extension-only shared into src/extension/shared/ | done | — | task_t002_shared_protocol_relocate | commit `410d1a4`；§4.3 表全闭合 |
| T007 | test: reorganize tests into unit/integration/e2e (Phase 4) | done | — | task_t002_shared_protocol_relocate | commit `3416dd6` + `70dde67`；三层 tests/{unit,e2e,support}/ |
| T008 | refactor: replace browser_no with browser_label + instance_id routing | done | — | main | commit `a408f24` + `89a88d4`；代码+文档；测试重写拆 T009/T010 |
| T009 | test: rewrite browser_label config/UI tests (partial) | done | — | main | commit `e97d451`；config_ui + settings_ui 重写 |
| T010 | test: rewrite agent_bridge_client/server tests for label routing | done | — | main | commit `12a55bb`；21+73 测试全绿；0 skip |
| T011 | security: remove Bridge token from argv in SessionStart hook | done | claude | task_t011_security_argv_token | commit `ebbb8ea`；project_01/HIGH-1；`.claude/settings.json` SessionStart 把 Bridge token 放进 `node --token "$token"` argv，对同机其他进程可见。改用环境变量前缀注入。 |
| T012 | security: broaden URL query redaction + fix redact_password precedence | done | claude | task_t012_redaction_rules_broaden | commit `31789b9`；src_bridge_mcp_shared_02/HIGH-4 + HIGH-6；`src/shared/redaction.ts` `SENSITIVE_URL_PARAMS` 精确匹配漏 access_token/api_key/大小写变体；`redact_password(enabled=false,password)` 返回明文违反 password 永不采集约束。改子串包含匹配 + password 判断优先于 enabled。 |
| T013 | privacy: keyboard shortcuts redaction + form_action URL + storage tab_id | done | claude | task_t013_content_capture_privacy | commit `83e307e`；src_extension_05/HIGH-1/2/3 + LOW-12；`keyboard_capture.ts:77` shortcuts 模式下 masked 恒 false；`form_submit_capture.ts:56` form.action 原样入库；`storage_capture.ts:104` tab_id 硬编码 0。修复：masked = redact_data；form_action 走 redact_url；storage_capture 加 tab_id 参数。 |
| T014 | privacy: WebSocket URL/header/payload redaction | done | claude | task_t014_network_ws_redaction | commit `41b4f05`；src_extension_02/HIGH-3 + src_extension_03/HIGH-7；`network_capture.ts` 与 `cdp_handler.ts` 的 WebSocket 路径绕过 redact_url/redact_headers，url_status/headers_status 写死 'captured'。三处（send_ws_connection_event / send_ws_frame / handle_ws_frame_error）的 url/headers 走脱敏。 |
| T015 | privacy: logger URL redaction + size limit | done | claude | task_t015_logger_redaction | commit `a4fbdde`；src_bridge_mcp_shared_02/HIGH-3；`src/shared/logger.ts:40-67` `Logger.write` 不脱敏、无大小限制；调用方 `content_script.ts:41`/`service_worker.ts:465,838,868` 把 `location.href` 等 URL 原样放日志。修复：Logger 边界统一对 details 递归脱敏 URL + 单条字节上限（如 64 KiB），不依赖调用方自律。 |
| T016 | privacy: honor capture_response_body config in CDP capture | done | claude | task_t016_capture_response_body_config | commit `0b016b7`；src_extension_02/HIGH-2；`src/extension/background/cdp_handler.ts:41-49,277-305,318-428` 声明 `capture_response_body` 配置但响应完成后无条件调 `Network.getResponseBody`，流式响应也无条件 `streamResourceContent`。修复：在流式识别与 loadingFinished 最前检查 capture_response_body，禁用时只发元数据并标 `not_enabled`。 |
| T017 | privacy: console/exception source filter + startup failure cleanup | done | claude | task_t017_console_exception_source_filter | commit `a39a233`；src_extension_02/HIGH-5 + HIGH-7；`console_capture.ts`/`exception_capture.ts` 忽略 `_source`、未调用 `should_handle_event`，跨 tab 采集；`chrome.dbg.attach` 成功后 `Runtime.enable` 失败 catch 不 detach，stop 因 `!is_capturing` 跳过。修复：复用 should_handle_event(source, tab_id)；启动 catch 中 best-effort detach 并重置状态。 |
| T018 | correctness: globally unique Bridge command IDs + COMMAND_CANCELLED | done | claude | task_t018_command_id_unique | commit `a4ea9a3`；src_bridge_mcp_shared_01/HIGH-1 + MEDIUM-5；`src/bridge/command_queue.ts:9` 每实例 queue 从 cmd_1 计数，`server.ts:58` `command_owners` 全局 map 仅按 command_id 索引，多实例首条命令碰撞。修复：命令 ID 改 `randomUUID()` 或全局序号；`AgentCommandQueue` 加 `cancel_all()` 用 `COMMAND_CANCELLED` resolve，实例顶替/stop 时调用。 |
| T019 | correctness: CDP events no-loss + CDP error terminal state | done | claude | task_t019_cdp_events_integrity | commit `4cd649c`；src_bridge_mcp_shared_01/HIGH-3/4；`src/bridge/cdp_handler.ts:311-325` `handle_cdp_events` 先全移 completed 再 slice(0,100)，101+ 永久丢；`{id,error}` CDP error 时事件永久 `pending`。修复：只移除本次返回的 100 条；CDP error 时设 `cdp_failed` 终态并清 body_seq_to_req_id 前 finalize 事件。 |
| T020 | correctness: input_event event_id via unified event creation | done | claude | task_t020_input_event_event_id | commit `a66aa35`；src_bridge_mcp_shared_02/HIGH-1；`src/extension/content/content_script.ts:236-258` 旧格式 `send_event(type,data)` 不调 `create_base_event`，dom_capture 全部 input 事件缺 `event_id`，IndexedDB keyPath 缺失触发 DataError。修复：删除旧格式分支或强制走 `create_content_event`；持久化边界补 event_id。 |
| T021 | correctness: WebSocket singleton listener + removeEventListener + UTF-8 byte size | done | claude | task_t021_websocket_capture_singleton | commit `2ef18bc`；src_extension_06/HIGH-1/2 + LOW-18；`src/extension/content/websocket_capture.ts:76-107` `onmessage` setter 与每个 `addEventListener('message')` 包装器都 post，重复采集；`removeEventListener` 无法移除 wrapper 导致 listener 泄漏；payload data_bytes 用 `.length` (UTF-16 code unit)。修复：单内部 listener 采集；保留原生 listener 语义；payload_bytes 用 `TextEncoder().encode().byteLength`。 |
| T022 | refactor: CDP state keyed by sessionId + requestId | done | claude | task_t022_cdp_state_keyed_by_session | commit `db9dc58`；src_extension_02/HIGH-1/4 + src_extension_03/HIGH-2；`src/extension/background/cdp_handler.ts` 与 `network_capture.ts` 的 `cdp_request_meta`/`cdp_body_results`/`streaming_requests`/`finished_before_stream`/`ws_connections` 仅按 requestId 索引，启用 `Target.setAutoAttach flatten:true` 后跨子目标可碰撞。修复：统一复合键 `${sessionId ?? 'root'}:${requestId}`，所有 Map/Set/deferred 索引贯穿复合键。 |
| T023 | correctness: cdp finished_before_stream / cdp_primary_emitted / orphan timer cleanup | done | claude | task_t023_cdp_state_cleanup | commit `9e6c31b`；src_extension_02/HIGH-8 + src_extension_03/HIGH-3/5 + MEDIUM-10；`cdp_handler.ts`/`network_capture.ts` 每条 `loadingFinished` 都 `finished_before_stream.add(req_id)`，普通请求永不删除；`cdp_primary_emitted` 同；orphan timer 不保存句柄无法取消；stop/reset 不清理。修复：仅在竞态窗口保存标记，正常完成后立即删除；orphan timer 跟踪并在 stop/reset 时清除。 |
| T024 | correctness: stream_buffer async backpressure + finish removes entry | done | claude | task_t024_stream_buffer_cleanup | commit `4c87714`；src_extension_04/HIGH-9/10 + LOW-21；`src/extension/background/stream_buffer.ts` `on_flush` 同步 void，flush 前清空 chunks 失败丢片段；flush 后不从 Map 删 entry 永久增长；`remove` 对不存在 request_id 抛 TypeError（`entry?.timer !== null`）。修复：on_flush 返回 Promise 串行；成功后才丢批次；finish 默认删除；remove 用 `entry?.timer != null`。 |
| T025 | correctness: loadingFailed main event + webRequest error + deferred cleanup | done | claude | task_t025_loading_failed_events | commit `28b4a5a`；src_extension_03/HIGH-4 + src_extension_04/MEDIUM-18；`network_capture.ts:595-603` `loadingFailed` 不构建主条目；`webrequest_handler.ts:188-192` `onErrorOccurred` 只删 pending 不输出失败事件；`network_context.ts:114-127` reset 不取消 deferred timer。修复：loadingFailed 已有 meta 时直接发失败主条目并清理；webRequest error 构造 NetworkRequestData 含 error_text；reset 前 clearTimeout 全部 deferred。 |
| T026 | correctness: SSE cumulative body byte cap | done | claude | task_t026_sse_body_cap | commit `38f95cf`；src_extension_03/CRITICAL-1；`network_capture.ts:190-197,475-479` stream_buffer 累计拼接到 `meta.response_body`，`max_body_capture_bytes` 仅在 loadingFinished 决定终态不限制采集期间累计，SSE 长连接可让 SW 内存持续增长。修复：累计达上限立即停止追加并标 `too_large`/`partial`，改有界 chunk 列表。 |
| T027 | correctness: HTTP redirect chain preservation | done | claude | task_t027_redirect_chain | commit `d2c097b`；src_extension_03/MEDIUM-8；`network_capture.ts:378-441` 重定向复用 request ID 时直接覆盖 `cdp_request_meta`，未先落盘前一跳也未处理 `params.redirectResponse`，中间跳转 Header/状态/耗时丢失。修复：识别 `redirectResponse`，覆盖前完成当前 hop；内部 hop 序号 + redirect chain。 |
| T028 | spike: SW capture state machine design | done | claude | task_t028_spike_sw_state_machine | commit `0739f3e`；src_extension_04/CRITICAL-1/2 设计；当前 `service_worker.ts` 用模块内存 `is_capturing`/`current_capture` 管状态，无串行化、SW 重启丢状态。Spike 输出状态机文档（`idle/starting/capturing/stopping`、generation token、持久化键设计），供 T029-T033 实施。 |
| T029 | refactor: SW start/stop serialization + state machine | done | claude | task_t029_sw_state_machine | commit `dcf60b2`；src_extension_04/CRITICAL-2（依赖 T028）；`service_worker.ts:267-327,479-558` start 仅入口检查 is_capturing 后多次 await，并发 start 可同时通过；stop 立即翻 is_capturing=false 可能拆掉新 start。实施状态机：starting 同步占位，stopping 完成前拒绝/排队 start。 |
| T030 | refactor: SW restart active capture recovery | done | claude | task_t030_sw_restart_recovery | commit `57a33a8`；src_extension_04/CRITICAL-1（依赖 T029）；`cleanup_stale_capture_state` 读 chrome.storage.local 的 `is_capturing`/`current_capture`，但 `start_capture`/`stop_capture` 都不写这两个键，Bridge 直接调 start_capture。修复：SW 自管持久化活跃状态，启动先恢复/终止旧采集，恢复前禁止处理 start。 |
| T031 | refactor: SW stop drain order (producers before state flip) | done | claude | task_t031_sw_stop_drain | commit `f44fae8`；src_extension_04/HIGH-5（依赖 T029）；`stop_capture` 先翻 is_capturing=false 写 stopped event + CaptureRecord completed，再停 network/body/cookie/console，所有回调看到 false 直接返回。修复：进入 stopping 拒新命令但继续接当前 generation 回调，先停生产者并 drain，再写 stopped + 最终 stats + flush + idle。 |
| T032 | refactor: SW start rollback on partial failure | done | claude | task_t032_sw_start_rollback | commit `7e3ecea`；src_extension_04/HIGH-6（依赖 T029）；`start_capture` 仅 `create_capture` try/catch，后续 attach/update_capture/body coordinator 抛错无回滚，留下半启动 + 永久 capturing 记录。修复：记录已完成步骤，异常时逆序停止 + flush + CaptureRecord 终态 + 清空状态。 |
| T033 | refactor: SW async listener generation check | done | claude | task_t033_sw_listener_generation | commit `14a29e6`；src_extension_04/HIGH-7（依赖 T029）；`service_worker.ts:745-929` tab listener 入口检查一次 is_capturing 后 await `chrome.tabs.get`/消息重试/CDP 启动，await 返回后不校验 generation，旧采集 callback 可写入新采集或重启已停子系统。修复：入口捕获 capture_id + generation，每个 await 后验证，事件构造用捕获值。 |
| T034 | fix: SW stop clears current_capture_id/start_time/config | done | claude | task_t034_sw_context_clear | commit `d86e326`；src_extension_04/MEDIUM-16；`service_worker.ts:554-558` stop 只清 `current_capture`，`current_capture_id`/`start_time`/`current_config` 未重置，`get_status()` 不检查 is_capturing 直接返回 `active_capture_id`。修复：stop 原子切换时清空全部上下文字段，Bridge 状态由状态机派生。 |
| T035 | fix: IndexedDB transaction commit boundary + flush retry | done | claude | task_t035_storage_transaction_atomic | commit `64c21ce`；src_extension_04/HIGH-4/12 + src_extension_01/MEDIUM-7；`storage.ts:146-199` create/update_capture 用 request.onsuccess resolve，未等 tx.oncomplete；`flush_store` 先 splice 再开 tx 失败丢批次；`app_log_storage.ts` 同模式；周期 flush 吞错。修复：写操作统一 tx.oncomplete resolve，onabort reject；失败保留批次有界重试；周期 flush 记录结构化错误。 |
| T036 | fix: delete_capture single transaction across stores | done | claude | task_t036_delete_capture_atomic | commit `e3a46c0`；src_extension_04/HIGH-8；`storage.ts:201-241` CaptureRecord + 8 类事件 9 个独立事务顺序删，任一失败留半删除；游标 `cursor.delete()` 无 error 处理，结束时 resolve 而非等 oncomplete。修复：单 readwrite 事务覆盖全部 store；oncomplete/onabort 统一监听；处理每个 delete 错误。 |
| T037 | fix: bytes_written persisted in CaptureRecord | done | claude | task_t037_bytes_written_persisted | commit `37652cf`；src_extension_04/MEDIUM-15；`storage.ts:263-264,368-373` `bytes_written` 是 SW 内存 Map，重启后归零；JSON 字符串 `.length` 不是 UTF-8 字节。修复：累计字节持久化到 CaptureRecord，tx 提交后更新，启动恢复时读取；用 TextEncoder 计字节。 |
| T038 | fix: storage buffer durability (persist before ack) | done | claude | task_t038_storage_buffer_durability | commit `7575607`；src_extension_04/HIGH-3；`storage.ts:263-317` 写入先入内存 buffer 不足 FLUSH_BATCH_SIZE 不落 IndexedDB，调用方收到成功；MV3 SW 回收丢内存批次但 stats 已增加。修复：每消息处理 promise 完成前保证数据入 IndexedDB（短生命周期事务或持久队列），buffer 只优化合并。 |
| T039 | feature: captures list search/filter/reset wiring | done | claude | task_t039_captures_search_filter | commit `968ba85`；src_extension_06/HIGH-4；`src/extension/dashboard/dashboard_captures.ts:52-71` 渲染 `#capSearch`/状态筛选按钮/`#capReset`，`wire_captures():102-132` 无 input/filter/reset 绑定。修复：搜索词与筛选写入持久 UI state，渲染前过滤 captures；绑定 input/change/click 事件，保留输入值与焦点。 |
| T040 | feature: exports page wiring | done | claude | task_t040_exports_page_wiring | commit `435a572`；src_extension_06/HIGH-5；`src/extension/dashboard/dashboard.ts:82-83` `page === 'exports'` 只 `innerHTML = render_exports()`，`dashboard_integrations.ts:29-39` 无 wire 函数，导出按钮无 click 绑定。修复：新增 `wire_exports()` 复用 `export_capture()`，处理进行中/成功/失败态，防重复点击。 |
| T041 | refactor: dashboard polling single-flight + incremental + a11y | done | claude | task_t041_dashboard_polling | commit `1c1d82d`；src_extension_06/MEDIUM-6/7/8/14/15/16/17/19；`dashboard.ts:113-132` `setInterval(async)` 不等前一轮可重叠；detail 每 2s 全量重建 DOM 丢 scroll/focus；`dashboard_detail.ts:155-169` 时间线 O(n²) `indexOf`；交互缺 ARIA/键盘/focus-visible；pulse 动画未适配 reduced-motion。修复：单飞轮询 + 推送/版本签名；详情增量更新；UI state 独立于 DOM；统一 ARIA/键盘/focus 样式。 |
| T042 | fix: navigation event table column schema | done | claude | task_t042_navigation_columns | commit `5c690ec`；src_extension_06/MEDIUM-11；`dashboard_detail.ts:99,388-402` navigation 表 6 表头但 `render_simple_events` 固定 5 列 grid，第六表头错位。修复：为各事件类型定义列 schema + 取值函数；navigation 输出 6 字段或减表头至 5。 |
| T043 | refactor: export pagination/streaming (replace 100000 silent truncation) | done | claude | task_t043_export_pagination | commit `42236c2`；src_extension_01/HIGH-3 + src_extension_02/HIGH-9；`agent_data_queries.ts:51-67` 与 `exporter.ts:21-43` 每类数据固定 limit 100000 静默截断，无 truncated 标记。修复：IndexedDB 游标分批/流式；必须上限时返回 `PAYLOAD_TOO_LARGE` 或分页游标 + `truncated: true`。 |
| T044 | security: HTML export escape all dynamic fields | done | claude | task_t044_html_escape | commit `518d50a`；src_extension_02/MEDIUM-18；`exporter.ts:104-167` JSON 已 escape，但 `capture_id`/`start_date`/`session.body_capture_mode`/`session.body_capture_status` 直接插值到 HTML text/title。修复：所有 HTML 文本节点统一调 HTML escape；用单一安全模板 helper。 |
| T045 | fix: HAR body size uses real bytes | done | claude | task_t045_har_body_size | commit `f2ca1ee`；src_extension_02/MEDIUM-20；`exporter.ts:258-306` `request.bodySize`/`response.content.size` 用 JS `.length` 字符数而非字节；base64 body 不区分 encoding。修复：优先 `request_body_bytes`/`response_body_bytes`；缺失时按 encoding TextEncoder 计字节；base64 明确 `content.encoding`。 |
| T046 | refactor: agent command result delivery retry + idempotency | done | claude | task_t046_result_delivery_retry | commit `b0200ff`；src_extension_01/HIGH-1；`agent_bridge_client.ts:150-167,303-315` 扩展取出命令并 dispatch 后 `send_result` 失败只写日志不重试，Bridge 已从队列移除命令最终超时，副作用命令（capture.start/stop）调用方收到 COMMAND_TIMEOUT。修复：按 command_id 持久化待投递结果队列，2xx 确认前持续重试并暂停拉取保证顺序；Bridge 端幂等确认。 |
| T047 | fix: browser_label sync to enrolled instance via heartbeat | done | claude | task_t047_browser_label_sync | commit `aaa19f1`；src_extension_01/HIGH-2；`agent_bridge_client.ts:196-205,273-282` 有 session 时直接复用 instance_token 不重新 enroll，heartbeat 只发 instance_id/版本/capture_id，配置改 label 后 Bridge 实例元数据保留旧标签。修复：heartbeat 携带规范化 browser_label，Bridge 原子更新；或检测配置变化后用 MCP token 重新 enroll。 |
| T048 | fix: agent dispatcher structured errors + offset/limit + unknown command | done | claude | task_t048_dispatcher_structured_errors | commit `5011f53`；src_extension_01/MEDIUM-8/9/10；`agent_command_dispatcher.ts:39-89` switch 无 default，未知 type 返回 ok:true,data:undefined；91-103 任一失败一律抛 RECORDING_ALREADY_RUNNING；116-175 offset/limit 接受负数小数。修复：未知 type 抛 INVALID_QUERY；handler 返回结构化 AgentErrorCode；offset/limit 校验非负整数与上限。 |
| T049 | fix: app_log pagination boundary + size estimate + flush retry | done | claude | task_t049_app_log_pagination | commit `d82e307`；src_extension_01/MEDIUM-5/6/7；`app_log_storage.ts:54-104` 终止条件 `counted >= limit + offset` 跳过 offset 后多返回 offset 条；`estimate_entry_bytes` 不计 details/stack 也不按 UTF-8 字节；flush 先 splice 再开 tx 失败永久丢批次。修复：跳过后以 `results.length >= limit` 终止；estimate 含全部字段按 UTF-8 字节；flush 失败放回批次有界重试。 |
| T050 | fix: body_capture_coordinator single-flight + lifecycle token | done | claude | task_t050_body_coord_single_flight | commit `641f7b1`；src_extension_01/HIGH-4；`body_capture_coordinator.ts:232-248,179-197` 500ms `setInterval(async)` 不等前一次，停止后已在途请求仍执行 `on_network_request` 无 lifecycle 校验。修复：完成后递归 setTimeout 单飞；维护 lifecycle token/AbortController，停止时取消请求并每批前验证 session key/capture_id/lifecycle。 |
| T051 | privacy: cookie listener scoped to target tab domain | done | claude | task_t051_cookie_scope | commit `330ca9c`；src_extension_02/HIGH-6；`cookie_capture.ts:34-87` `chrome.cookies.onChanged` 全局事件，未按目标 tab/site/store 过滤，所有事件写 tab_id:0。默认决策：限定到目标 tab domain。修复：启动时传入目标 tab/site/store，按 cookie domain/path 与导航范围过滤；无法可靠归属时默认丢弃。 |
| T052 | security: external bridge URL allowlist (127.0.0.1 only) | done | claude | task_t052_external_bridge_url_allowlist | commit `e3fe830`；src_extension_02/HIGH-10；`external_cdp_bridge_client.ts:6-10,39-54,73-147` 直接拼 `config.bridge_url` 不验证 scheme/hostname/port/path。默认决策：仅允许 `http://127.0.0.1:<port>`。修复：构造请求前解析 URL，拒绝非 http(s)、非 127.0.0.1/localhost、含凭据/fragment/额外 path。 |
| T053 | fix: self_origin_url filter precise to configured Bridge origin | done | claude | task_t053_self_origin_filter | commit `272f5b9`；src_extension_02/MEDIUM-11 + src_extension_04/MEDIUM-19；`cdp_handler.ts:212-218,705-716` 与 `webrequest_handler.ts:36-38,325-335` 把所有 `127.0.0.1`/`localhost` URL 视为自身流量。修复：只排除扩展 origin 与配置 Bridge origin 的精确端口/path；用规范化 origin 比较。 |
| T054 | fix: webRequest response body bytes/encoding populated | done | claude | task_t054_webrequest_body_bytes | commit `1d20788`；src_extension_04/MEDIUM-20；`webrequest_handler.ts:242-247` build_network_event 无论 body 是否存在都设 `response_body_encoding: null`/`response_body_bytes: null`，service_worker.ts:707-11 依赖该字段累加 total_body_bytes。修复：按 body result 真实编码填 encoding；按编码后字节长度填 bytes。 |
| T055 | fix: HTTP headers preserve multi-value (Set-Cookie etc) | done | claude | task_t055_headers_multi_value | commit `d6f3c51`；src_extension_03/MEDIUM-12；`network_webrequest.ts:78-85` 与 `network_capture.ts:765-767` Header 数组按 name 直接赋值，重复字段被后值覆盖；`Set-Cookie`/`Warning`/`Link` 允许重复。修复：数据模型允许时用 `Record<string, string[]>`；必须字符串时按语义合并，`Set-Cookie` 保留数组。 |
| T056 | fix: network_correlator merge_matched uses non-empty check | done | claude | task_t056_correlator_merge | commit `d702daa`；src_extension_03/MEDIUM-13；`network_correlator.ts:80-93` `web_meta.request_headers || cdp_event.request_headers`，空对象 `{}` 为真值不回退到 CDP Header。修复：按 `Object.keys(headers).length > 0` 判断可用，或合并两源定义冲突优先级。 |
| T057 | refactor: error code term migration (SESSION/RECORDING → CAPTURE, alias compat) | done | claude | task_t057_error_code_aliases | commit `09c92e4`；src_bridge_mcp_shared_02/HIGH-2；`protocol.ts:17-37` 与 `agent_command_dispatcher.ts:91-113` 仍返回 `SESSION_NOT_FOUND`/`RECORDING_ALREADY_RUNNING`/`NO_ACTIVE_RECORDING` 旧术语，与领域文档要求的 capture 术语冲突。默认决策：保留旧码 + 加新码别名（渐进迁移，避免 breaking）。修复：协议新旧码均声明，dispatcher 优先返回新码，标注废弃期限。 |
| T058 | refactor: event_category exhaustive mapping | done | claude | task_t058_event_category_exhaustive | commit `d852e9e`；src_bridge_mcp_shared_02/MEDIUM-8；`event_category.ts:7-17` 漏 `capture_config_changed`/`permission_missing`/`debugger_attach_status`/`body_capture_status_changed` 4 类合法生命周期事件，未知类型默认 `dom_data`。修复：用 `Record<EventType, CategoryKey>` 或 exhaustive switch，编译期强制完整映射。 |
| T059 | refactor: event_id uses crypto.randomUUID | done | claude | task_t059_event_id_uuid | commit `afd7554`； src_bridge_mcp_shared_02/MEDIUM-5；`event_utils.ts:4-20` event_id = `Date.now()+Math.random()*1e6+counter`，不是 UUID；多 frame/SW 重启 counter 归零可碰撞。修复：优先 `crypto.randomUUID()`；旧环境 fallback `crypto.getRandomValues()` 实现_UUID v4。 |
| T060 | refactor: UserConfig runtime schema validation | done | claude | task_t060_user_config_schema | commit `7375775`； src_bridge_mcp_shared_02/MEDIUM-7；`user_config.ts:377-408` `load_user_config` 直接浅合并 + `as UserConfig`，除 timezone 外不校验 enum/boolean/数值范围/URL/label 长度。修复：单一 normalizer 加载时逐字段校验回退默认，save 拒绝无效 patch。 |
| T061 | refactor: CDP target URL strict match (no silent fallback) | done | claude | task_t061_cdp_target_strict | commit `df1c690`； src_bridge_mcp_shared_01/HIGH-2；`src/bridge/cdp_handler.ts:111-114` tab_url 精确匹配失败后自动退回首个 page target 仍返回 ok:true，可能采集错误页面。修复：tab_url 非空且无匹配时 fail fast 返回 `cdp_target_not_found`，禁止退回其他页面。 |
| T062 | fix: /cdp/start waits for WebSocket open + Network.enable | done | claude | task_t062_cdp_start_connect_status | commit `5e97af7`；src_bridge_mcp_shared_01/MEDIUM-7；`src/bridge/cdp_handler.ts:131-140,275-296` 创建 WebSocket 后立即写 session 并返回 ok:true，未等 open/未设连接超时，error/close 只设 cdp_ws=null 调用方仍认为启动成功。修复：等 open 或 error/timeout 后再返回；失败删 session 返回 `cdp_start_failed`。 |
| T063 | fix: max_body_capture_bytes + bridge timeout boundary validation | done | claude | task_t063_boundary_validation | commit `492a37e`； src_bridge_mcp_shared_01/MEDIUM-6 + LOW-9；`src/bridge/cdp_handler.ts:87-95,118-129` `max_body_capture_bytes` 接受任意 number 不校验整数/非负/上限；`server.ts:787-808` `validate_command_request` 只查 timeout_ms 类型不校验正整数/上限。修复：HTTP 边界 `Number.isSafeInteger` 与 `0..MAX_BODY_CAPTURE_BYTES`，timeout_ms `> 0 && <= 上限`，无效值返回 INVALID_QUERY。 |
| T064 | security: persisted token file permission verify/chmod | done | claude | task_t064_token_file_perm | commit `19e7d37` | src_bridge_mcp_shared_01/MEDIUM-8；`src/bridge/config.ts:81-95,103-123` 新建 token 文件 mode 0600，但读已有文件前不 stat 检查权限；`writeFile(mode:0o600)` 对已存在文件不收紧权限。修复：读取前 stat 拒绝/修复非 0600；写入后显式 `chmod(0o600)`；更稳妥排他临时文件 + 原子 rename。 |
| T065 | fix: ws_connections cap + clear on capture stop | done | claude | task_t065_ws_connections_cap | commit `19e7d37` | src_extension_05/MEDIUM-7 + LOW-11；`src/extension/background/ws_handler.ts:17,138` `ws_connections` Map 仅在 close 时删，CDP 不发 close（tab 崩溃/网络中断）条目永久残留；params 全 `any` 类型。修复：采集 stop 时清空 ws_connections 或加 LRU 上限；定义 CDP WebSocket 事件最小接口类型。 |
| T066 | fix: network_hook UTF-8 safe truncation | done | claude | task_t066_network_hook_multibyte | commit `19e7d37` | src_extension_05/MEDIUM-6；`src/extension/content/network_hook.ts:79-83` 注入脚本 `TextEncoder.encode(text)` 计字节但截断用 `text.slice(0, MAX)` 按字符切再 `TextDecoder.decode(bytes.slice(0, MAX))`，多字节字符可能被切开产生 U+FFFD。修复：统一用 `TextDecoder().decode(bytes.slice(0, MAX))` 作为截断结果。 |
| T067 | fix: mouse wheel/drag target info + keyboard target_input_type | done | claude | task_t067_mouse_wheel_drag_target | commit `19e7d37` | src_extension_05/LOW-10 + LOW-12；`mouse_capture.ts:134-165` wheel/dragstart/dragend target.text 设 null 而 click 用 get_target_info；keyboard target_input_type 已在 T013 修复，此处仅 mouse wheel/drag 统一调 get_target_info(event)。 |
| T068 | fix: keepalive listener idempotent | done | claude | task_t068_keepalive_idempotent | commit `19e7d37` | src_extension_02/LOW-22；`src/extension/background/keepalive.ts:20-26` 每次 `setup_keepalive_listener` 注册新匿名 listener，无法检查/移除。修复：模块级命名 handler + `listener_registered` 标志，setup 幂等。 |
| T069 | fix: deferred body association disambiguation (mark ambiguous, no guess) | done | claude | task_t069_deferred_disambiguation | commit `30010a4` | src_extension_03/MEDIUM-9 + src_extension_04/HIGH-11；`network_capture.ts:682-723` 与 `webrequest_handler.ts:104-185` deferred 关联仅按 base URL + method + status 选候选，多候选时把"最后触发解析"body 分给 entry；webRequest 匹配无 tab/frame/时间。修复：候选评分含 tab/session/完整 URL/时间差；歧义候选 >1 时标记 `ambiguous` 不附 body。 |
| T070 | fix: external bridge response schema validation + session_key in header | done | claude | task_t070_external_bridge_response_schema | commit `30010a4` | src_extension_02/MEDIUM-15/16/17；`external_cdp_bridge_client.ts:56-63,102-106,126-128` `res.json()` 直接信任未验证；HTTP 非 2xx/超时/认证失败/JSON 解析失败统一返回空数组伪装"无事件"；`session_key` 在 URL query 易进日志。修复：返回判别联合（empty/transient/auth/invalid）；所有响应边界 schema 校验 + 体量限制；session_key 改 header 或 POST body。 |
| T071 | security: WebSocket capture per-page nonce + strict schema | done | claude | task_t071_websocket_nonce | commit `30010a4` | src_extension_06/HIGH-3；`src/extension/content/websocket_capture.ts:11-16,49-56` content script 仅校验 origin + source===window + 固定字符串 `__capture_all_ws__`，页面任意脚本可 postMessage 伪造事件。默认决策：保留 inline script 注入，加 per-page nonce。修复：注入时生成不可预测 nonce，通过 MessageChannel 传递；严格校验字段类型/枚举/长度/URL；速率与数量限制。 |
| T072 | fix: WsFrameData.session_id deprecated alias | done | claude | task_t072_ws_session_id_deprecated | commit `27ebc6e`；src_bridge_mcp_shared_02/LOW-10；`src/shared/types.ts:366-379` `WsFrameData.session_id` 仍是普通可选字段无 `@deprecated`。修复：线协议若仍需兼容则加 `capture_id` 主字段并将 session_id 标 deprecated，单一适配边界转换；无需兼容则直接改 `capture_id`。 |
| T073 | test: T008 residual browser_no tests cleanup + finalize archive | done | claude | task_t073_t008_finalize | commit `13ce036`； docs_01/HIGH-03 + MEDIUM-14/15；`tests/unit/agent_mcp_client.test.ts:334-350` 与 `mcp_schema.test.ts:257-273` 仍含 browser_no 兼容测试（非纯注释）；T008 index 标 done 但 spec 验收未勾选、缺 log/review/adoption 工件、目录未归档；T010 commit pending 字段已过时（实际 12a55bb）。修复：清理 browser_no 残留测试或转为说明性注释；补 T008 log/review/adoption 并归档；index T010 更新 commit sha。 |
| T074 | docs: README 64 MiB + refactor_plan reference cleanup | done | claude | task_t074_readme_and_refs | commit `8845c2f`； project_01/MEDIUM-1/2；`README.md:217`/`README.en.md:216` 写"扩展结果回传上限 32 MiB"，实际 `src/bridge/server.ts:42` MAX_EXTENSION_RESULT_BODY_BYTES = 64 MiB，SECURITY.md:40 也是 64 MiB，`tests/unit/public_docs.test.ts:81` 固化错误数字；`AGENTS.md:20`/`CLAUDE.md:20` 要求修改 src 前必读 `docs/refactor_plan.md` 但该文件不存在。修复：README 改 64 MiB；公开文档测试从实现常量校验；删除 refactor_plan.md 导航，有效约束归 blueprint。 |
| T075 | docs: AGENTS/CLAUDE/SECURITY/decisions token model unification | done | claude | task_t075_token_model_unification | commit `c1a795a`； project_01/MEDIUM-3/4 + docs_01/MEDIUM-05；`AGENTS.md:141`/`CLAUDE.md:141` 先声明 token 优先级含 `persisted > generated` 后写"用户提供"自相矛盾；`SECURITY.md:36-38` 称所有数据端点用 user-provided token 与当前双 token 模型（MCP token + instance_token）冲突；domain.md 重复冲突。修复：统一表述"禁止硬编码/默认/示例值；允许本地安全随机生成 mode 0600 持久化或用户显式提供"；SECURITY 区分 MCP token 与 instance_token 来源/路由/存储。 |
| T076 | docs: deployment guide bridge --port + token + log location | done | claude | task_t076_deployment_guide | commit `b6ed532`； docs_01/HIGH-05/06 + MEDIUM-09；`docs/guides/deployment.md:33-38,44-55` `npm run bridge`/systemd ExecStart/node bridge.mjs 均未传 `--port`，main.ts 抛 Invalid bridge port；34/51-54 给固定 token 占位值 'your-random-token' 违反 secret 约束；85-89 列 `artifacts/bridge/bridge.log` 但代码只写 stdout/stderr。修复：所有启动示例显式 `--port`；token 改为"用户用密码管理器生成 + EnvironmentFile 注入"；日志改"前台看 stdout/stderr，systemd 用 journalctl"。 |
| T077 | docs: mcp_usage guide rewrite (browser_no -> browser_label/pairing) | done | claude | task_t077_mcp_usage | commit `f0eb07f`； docs_01/HIGH-04 + MEDIUM-10；`docs/guides/mcp_usage.md:8-13` 仍指导配置/批准/传递已移除的 browser_no；104-123 示例 mouse_precision:clicks、关闭 body、1 MiB 与默认值差异大。`troubleshooting.md:55` 同。修复：重写快速开始 step 2/5/7（可选 browser_label + pairing code + target_label/target_instance_id）；示例标题改"最小化示例"或对齐默认值并标注。 |
| T078 | docs: contributing_dev full rewrite against current tree | done | claude | task_t078_contributing_dev | commit `6af4a44` | docs_01/MEDIUM-08；`docs/guides/contributing_dev.md:31-225` 仍展示已删 `src/agent`、顶层 `src/background`/`content`、根 `e2e`、`docs/refactor_plan.md`；Bridge 误称 WebSocket；Logger 示例错用 `../shared/logger` 且缺 transport 参数；vitest 示例指向 `tests/*.test.ts`；构建产物漏 extension.zip。修复：按 architecture.md 当前目录整体重写；示例分别用 background IndexedDB transport 与 content MessageLogTransport。 |
| T079 | docs: test.md rebuild from package.json/vitest/playwright | done | claude | task_t079_test_guide | commit `6af4a44` | docs_01/MEDIUM-11；`docs/guides/test.md:17-154` Vitest 写 2.x（实际 4.x）、测试树平铺（实际 tests/{unit,e2e,support}）、vitest exclude 漏 tests/e2e 与 tests/support、Bridge/MCP 路径 src/agent/...、build 描述漏 copy:locales/build:zip、Playwright testDir ./tests（实际 ./tests/e2e）、`e2e-mcp*.spec.ts` 误含 mcp-full。修复：直接从 package.json/vitest.config.ts/playwright.config.ts 重建命令与项目表。 |
| T080 | docs: blueprint cleanup (architecture/domain/conventions/decisions broken links + counts) | done | claude | task_t080_blueprint_cleanup | commit `a6a9c3a` | docs_01/MEDIUM-01~07 + LOW-01；`architecture.md:73-92` 后台目录漏列 cdp_handler/webrequest_handler/ws_handler；:109-110,237-242 把 devtools_panel 误列为 Vite 入口（实际 vite.config.ts 12-19 不含）；:245-246/domain:29-48 写 12 个 MCP 工具实际 17（含 4 别名）；domain 错误码表用未上线的 SESSION/RECORDING 等新名（应先记当前 wire code）；conventions:30,68,78-93 设计令牌/MCP schema/测试路径错；多处 `specs/...`/`docs/refactor_plan.md` 断链。修复：按当前 src 实况与 src/mcp/tools.ts 重建工具表；活跃蓝图用完整现行路径，历史归档明确 docs/archive。 |
| T081 | docs: troubleshooting focuses on active capture/content script first | done | claude | task_t081_troubleshooting | commit `b6ed532`； docs_01/LOW-02；`docs/guides/troubleshooting.md:16-27` 整体无事件排查直接要求确认 `capture_response_body`，但该配置只影响响应体。修复：先检查 active capture、content script/SW 消息、站点限制、各类别开关；仅"缺响应体"场景检查 capture_response_body。 |
| T082 | docs: store_publish_list PII disclosure + icon path + storage permission | done | claude | task_t082_store_publish_list | commit `24812ad`； docs_01/HIGH-07 + MEDIUM-12；`docs/guides/store_publish_list.md:115-130` 要求 Edge 商店不勾选 PII/Authentication/Personal communications 并断言"未勾选均不采集"，但默认 capture_input_values/capture_request_body/capture_response_body=true，PRIVACY.md 也声明可能含 credentials/tokens/private messages；61-65 称 session metadata 存 chrome.storage.local 不准确（实际 IndexedDB 存采集，storage.local 存设置/状态）；150-155 引用 artifacts/dist/assets/icons/icon300.png 但 manifest 只引用 16/32/48/128。修复：删除"未勾选均不采集"绝对断言，让披露与 PRIVACY.md 同步；权限说明改"持久化设置和运行状态"；商店图标直接引用源文件 assets/icons/icon300.png 或加构建复制步骤。 |
| T083 | docs: handoff append post-T008 status | done | claude | task_t083_handoff | commit `33027a3`； docs_01/MEDIUM-13；`docs/handoff.md:23-49` 唯一真实交接停在 task_t002_shared_protocol_relocate"仅剩 merge main"，当前 main 已含 70dde67 及 T008-T014 提交。修复：保留历史段落不改，追加新段说明 T002 已合入 main、当前 head 与 T008-T014 进展。 |
| T084 | docs: PR template narrow .claude scope | done | claude | task_t084_pr_template | commit `a33be44`； project_01/LOW-1；`.github/pull_request_template.md:17` 要求 PR 不含任何 `.claude/`，但仓库跟踪 `.claude/settings.json`，`scripts/scan_tracked_tree.mjs:23-24` 允许项目级 settings.json 只禁 settings.local/skills/worktrees。修复：改为禁 `.claude/settings.local.json`/`.claude/skills/`/`.claude/worktrees/`，明确 settings.json 必要时可提交。 |
| T085 | tooling: scan_tracked_tree skip docs/archive + AST-based credential detection | done | claude | task_t085_scan_archive_skip | commit `61e3481`； project_02/HIGH-1/2；`scripts/scan_tracked_tree.mjs:23-26,284-292` 注释说"scanner skips docs/archive"但 forbidden_paths 未含 archive 路径，CI 71 项 finding 大量来自 docs/archive；67-109,257-281 `assignment_pattern` 只截空白前 token 再正则猜测，把 `src/bridge/server.ts:282` 模板字符串随机生成 `instance_token = \`ext_${randomBytes(...)}\`` 与测试 fixture 误报为 credential-assignment。修复：文件枚举后显式 `path.startsWith('docs/archive/')` 跳过；源码改 AST（或完整表达式归类为非硬编码）；为当前误报加回归测试。 |
| T086 | tooling: vite build time deterministic + indent + zip dep declare | done | claude | task_t086_vite_build_time | commit `ea329f5`； project_02/MEDIUM-5/LOW-3/LOW-6；`vite.config.ts:6-8` 构建时间 = `Date.now()+8h` 再 toISOString 删时区，无时区标识且每次构建不同；:10-21 rollupOptions 缩进少一级；`package.json:24,39` `build:zip` 调系统 zip 但文档未声明依赖。修复：用 package.json 版本或 commit SHA；必须显示构建时间则用 Asia/Shanghai + UTC+8 标识，CI 注入 SOURCE_DATE_EPOCH；修缩进；在 README/CONTRIBUTING 声明 zip 前置或用 Node 库。 |
| T087 | tooling: store screenshots output path + outline_svg font_family | done | claude | task_t087_store_screenshots | commit `45fff4a` | project_02/MEDIUM-4/LOW-7；`scripts/capture_store_screenshots.mjs:9,13-16` 固定写 `store/screenshots/*.png` 但 .gitignore 未覆盖；`scripts/outline_svg_text.py:56-66,116-124` `pick_font(text, font_family)` 接收 font_family 但函数体不读，注释声称 CJK 策略未实现。修复：截图改 `artifacts/store/screenshots/` 或纳入 .gitignore；要么解析允许列表用 font_family 选字体，要么移除参数与误导注释。 |
| T088 | tooling: Playwright reuseExistingServer=false in CI | done | claude | task_t088_playwright_reuse | commit `45fff4a` | project_02/MEDIUM-3；`playwright.config.ts:25-37` 两个 webServer `reuseExistingServer: true`，健康检查只验固定 URL 可达不验当前仓库/构建版本。修复：CI 强制 `reuseExistingServer: !process.env.CI ? true : false`；fixture 加唯一健康标识或版本响应，启动前校验。 |
| T089 | docs: task spec template enhancements (AC format, decisions, constraints) | done | claude | task_t089_task_spec_template | commit `80c773c`； docs_02 全部；`docs/templates/task/spec.md:15-17` 验收标准占位只要求"可独立验证的完成条件"未要求验证方式/可观察结果；缺"已确认决策/待确认问题"章节；"依赖与约束"提示过宽未带项目硬约束清单；范围占位未要求按代码/测试/文档/迁移划分；标题无 `{TNNN} {slug}` 占位；未提示单 task 单结果。修复：AC 改"条件/验证/预期"格式；增加可选"已确认决策/待确认问题"；约束分"前置依赖/受影响不变量/数据兼容/平台限制"；范围提示检查代码/测试/文档/迁移/产物；标题统一 `# Task spec — {TNNN} {slug}`；范围前加单 task 单结果说明。同步 repo_template 上游。 |
| T090 | refactor: export memory streaming (avoid full materialization) | done | claude | task_t090_export_streaming | commit `30010a4` | src_extension_02/MEDIUM-19；`src/extension/background/exporter.ts:21-178,376-392` 各 store 数据并行加载为数组，再合并 + 系统时间副本 + JSON/HTML 字符串，JSONL 也先累计全部 lines 非流式。MV3 SW 内存压力 + 长任务终止。修复：分页游标 + 流式归档/写出；避免 Promise.all 同时拉全表；HTML 可只嵌压缩归档。 |
